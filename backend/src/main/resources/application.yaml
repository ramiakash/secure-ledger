springdoc:
  api-docs:
    enabled: ${SWAGGER_ENABLED:true}
  swagger-ui:
    enabled: ${SWAGGER_ENABLED:true}
ledger:
  jwt:
    secret: ${JWT_SECRET:gPLBo6diw8ef+Akezym6geqyBjEO1Kbq+ipxSH1QF/4=}
    issuer: secure-ledger
    access-token-ttl: PT1H
spring:
  application:
    name: ledger
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5433}/${DB_NAME:ledger}
    username: ${DB_USER:ledger_app}
    password: ${DB_PASSWORD:ledger_local_pw}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false